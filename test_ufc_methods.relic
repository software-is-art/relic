// Test UFC syntax with methods

method double(x: Int) -> Int {
    x * 2
}

method triple(x: Int) -> Int {
    x * 3
}

method concat(x: String, suffix: String) -> String {
    x  // Just return x for now since we don't have string concat
}

fn test_ufc() -> Int {
    // Test UFC with methods
    let x = 5 in
    x.double()
}

fn test_chaining() -> Int {
    // Test method chaining
    7.double().triple()
}